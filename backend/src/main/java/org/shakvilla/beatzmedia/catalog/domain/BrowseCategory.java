package org.shakvilla.beatzmedia.catalog.domain;

/** Domain value: a browseable music genre / mood category. Catalog ADD §7, WU-CAT-2. */
public record BrowseCategory(String id, String title, String colorClass) {}
